{
  "name": "breakfast-pos",
  "version": "1.0.0",
  "private": true,
  "description": "Breakfast POS monorepo",
  "workspaces": [
    "backend",
    "frontend",
    "admin"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend,admin -c blue,green,magenta \"npm run dev -w backend\" \"npm run dev -w frontend\" \"npm run dev -w admin\"",
    "build": "npm run build -w backend && npm run build -w frontend && npm run build -w admin",
    "test:smoke": "node scripts/smoke-test.mjs",
    "test:system": "node scripts/system-verification.mjs",
    "test:ui": "node scripts/ui-verification.mjs",
    "qa:full": "npm run test:smoke && npm run test:system && npm run test:ui",
    "test:smoke:gateway": "cross-env SMOKE_BASE_URL=http://localhost:8080/api node scripts/smoke-test.mjs",
    "test:system:gateway": "cross-env SYSTEM_TEST_BASE_URL=http://localhost:8080/api node scripts/system-verification.mjs",
    "test:ui:gateway": "cross-env UI_TEST_FRONTEND_URL=http://localhost:8080 UI_TEST_ADMIN_URL=http://localhost:8080/admin node scripts/ui-verification.mjs",
    "qa:gateway": "npm run test:smoke:gateway && npm run test:system:gateway && npm run test:ui:gateway",
    "docker:build": "docker compose build",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down",
    "db:generate": "npm run prisma:generate -w backend",
    "db:migrate": "docker compose up -d postgres && docker compose build backend && docker compose run --rm backend sh -c \"npx prisma migrate deploy\"",
    "db:seed": "docker compose up -d postgres && docker compose build backend && docker compose run --rm backend sh -c \"npx prisma generate && node prisma/seed.js\"",
    "setup": "npm install && npm run build && npm run db:migrate && npm run db:seed"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3",
    "playwright": "^1.58.2"
  }
}
